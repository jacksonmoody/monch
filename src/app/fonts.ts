import { Titan_One, Inter } from "next/font/google";

export const titan = Titan_One({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});
